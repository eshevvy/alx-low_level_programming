#include <stdio.h>
/**
 * main - function that prints the phrase "with proper grammer, but the outcome
 * is a piece of art,"
 * Return: gives a return value of 0 when main executes properly, non-zero
 * otherwise.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,/");
	return (0);
}
